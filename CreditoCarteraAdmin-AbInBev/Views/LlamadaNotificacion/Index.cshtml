@model IEnumerable<CreditoCarteraAdmin_AbInBev.Models.LlamadaNotificacion>

@{
    ViewData["Title"] = "Index";
}

<h2 class="text-white">Configuración de llamadas</h2>

@*<p>
    <a asp-action="Create">Crear nuevo</a>
</p>*@
<table class="table text-white-50">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BlobStorageURL)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiasPrevios)
            </th>
            <th>
                ¡Escucha!
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BlobStorageURL)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiasPrevios)
            </td>
            <td>
                <audio src= @item.BlobStorageURL controls="controls">
                    Tu navegador no soporta el elemento
                    <code>audio</code>
                </audio>
            </td>
            <td>
            </td>
            @*<td>
            <a asp-action="Edit" asp-route-id="@item.Id">Modificar</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Borrar</a>
        </td>*@
        </tr>
}
    </tbody>
</table>

<a asp-controller="Home" asp-action="Configuracion">Regresar</a>
